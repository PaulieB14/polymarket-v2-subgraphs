{
  "name": "polymarket-v2-main-subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "Polymarket CLOB V2 main subgraph — conditions + resolution status",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio polymarket-v2-main",
    "create-local": "graph create --node http://localhost:8020/ polymarket-v2-main",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 polymarket-v2-main",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2",
    "source-map-support": "^0.5.21"
  }
}
