{
  "name": "polymarket-v2-orderbook-subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "Polymarket CLOB V2 orderbook subgraph — CTF Exchange V2 + Neg Risk Exchange V2",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio polymarket-v2-orderbook",
    "create-local": "graph create --node http://localhost:8020/ polymarket-v2-orderbook",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 polymarket-v2-orderbook",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.39.0"
  }
}
